import React from 'react';
import { useSimulator } from '../context/SimulatorContext';

const SandboxMode = () => {
    // Lógica para el modo sandbox
    return (
        <div>
            {/* Componentes del modo sandbox */}
        </div>
    );
};

export default SandboxMode;